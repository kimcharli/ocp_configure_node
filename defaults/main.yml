---
# defaults file for ocp_configure_node
ansible_become: true

node_type: compute

# Storage values in GB
min_storage:
  master: 50
  infra: 50
  compute: 50

# Memory values in GB
min_memory:
  master: 16
  infra: 16
  compute: 32

min_cpus:
  master: 4
  infra: 2
  compute: 8

docker_device: vdb
